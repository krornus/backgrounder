error.rs
    Find a better way to derive serde
    Errors hold strings for easy serialization

playlist.rs
    Decide if next/prev should panic
    - probably yes

main.rs - converse
    support polling for server lock

main.rs
    option to clear list when adding a path (-c?)
    gives append/set functionality

controller.rs/playlist.rs
    save to file functionality

config.rs
    config file suppport

controller.rs
    interval thread
    should poll every ~2s or something
    load new value
    next if new value < old
